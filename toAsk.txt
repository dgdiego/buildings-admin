Retorno de promise en el catch. Devuelve el objeto Promise

Al agregar un edificio muestra el editar

Al ingresar a los pagos del apartamento, hay que refrescar la página para que muestre el contenido

Pasar propiedades entre componentes con el LinkTo


npm install bcrypt -g
npm install bcrypt --save

//const bcrypt = require('bcrypt');
